/**
 * Root component for the Decision Flow Builder Issue Panel
 * 
 * This component serves as the main container for the issue panel UI.
 * It will be responsible for:
 * - Retrieving the current issue context using @forge/bridge
 * - Loading applicable flows for the issue's project
 * - Managing the selected flow and current view state
 * - Rendering flow tabs and view components
 * 
 * This is a placeholder implementation that will be expanded in subsequent tasks.
 */

import React from 'react';

function App() {
  return (
    <div style={{ padding: '20px' }}>
      <h1>Decision Flow Builder - Issue Panel</h1>
      <p>Issue panel setup complete. Flow tabs and views will be implemented in subsequent tasks.</p>
    </div>
  );
}

export default App;
